/** @type {import("src").ConfigInput} */
const config = {
  name: "icon-lib",
  prefix: "z",
  collections: [
    {
      type: "FONT",
      name: "icon",
      prefix: "i",
      input: "icons",
      output: "icons",
    },
  ],
};

export default config;
